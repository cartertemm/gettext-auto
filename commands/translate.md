---
description: Translate a gettext project to <lang> via the gettext-auto skill.
---

Translate this project's strings to `$ARGUMENTS` using the gettext-auto skill.

Invoke the `gettext-auto` skill and follow its protocol with target language = `$ARGUMENTS`.
